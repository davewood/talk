Talk is an application to write and display presentation slides.

Slides are written in HTML and the author can use jQuery and Bootstrap.

Run script/talk_server.pl to run the application from a terminal.
